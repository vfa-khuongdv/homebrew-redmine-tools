# Alternative workflow section using GitHub CLI
- name: Update Homebrew formula (Alternative)
  if: startsWith(github.ref, 'refs/tags/v')
  env:
    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  run: |
    # Use GitHub CLI to clone (automatically handles auth)
    gh repo clone vfa-khuongdv/homebrew-redmine-tools homebrew-tap
    cd homebrew-tap
    
    # Configure git
    git config user.name "GitHub Actions"
    git config user.email "actions@github.com"
    
    # Your update logic here
    # ... (rest of the script)
